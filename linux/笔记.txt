from collection.abc import sequence
ctrl c 终止命令ctrl shift c复制
ctrl d 退出终端
ctrl l清屏
a跳到行首e跳到行尾
z暂停
Ctrl + U：在终端中，剪切光标之前的所有内容
w删一个词
cd..上级目录
cd~家目录
/根目录
-上一级目录
空格分隔指令
注意大小写，‘“
#注释
Tab补全
ls list
-a 显示包括隐藏
-h人性化显示
-R递归显示
-l长格式 
mkdir创建路径
mldir dir1 dir2 dir3
-p递归创建
./当前路径  我在那
~/家路径 普通用户 /home/xxx  root /root  我是谁
echo回声
-e识别转义符
-E不自动识别转义符
-n不添加换行符
touch创建文件 更新时间戳
-c只更新时间戳
-d指定时间

head开头
 tail结尾
head -n指定行数

mv 移动，重命名
-i有提示覆盖
-f强制覆盖
-n不覆盖
-v详细显示

cp copy
cp -r递归粘贴 （路径）

rwx 阅读写执行 八进制 分三组
chmod修改权限
-u所有者
-g所属组
+-=
chmod 777 file.txt
生成公钥私钥ssh-keygen
id_rsa -》 私钥
id_rsa.pub

sha256
wget下载
curl -O本地 -L重定向

find查找 -type
0标准输入
1标准输出
2标准错误
find / -name "*.py" > 1.txt 2>&1查找全系统的Python文件，并将结果（包括错误）保存到 1.txt 中

alias化名
.bashrc  .隐藏  登录式配置文档
.bash_profile非登录式.bash_login .profile
/usr /usr/bin /usr/local/bin /usr/sbin /usr/lib
source .追加
/var可变数据 /log日志 /cache缓存
/dev硬件设备和设备驱动
/tmp临时文件
/boot系统启动的引导文件
export 临时设置
软件安装apt rpm dnf yum 换源

skill.md限制
node cargo
shell安装
#! /bin/bash
常见架构X86 AMD64 X86_64 ARM RISC-V
编译执行和解释执行

javascript js JavaScript 是一种用于网页开发的脚本语言，主要用于实现网页的交互功能和动态效果。

npm 是 Node Package Manager 的缩写，是 Node.js 的包管理工具，用于安装、更新和管理 JavaScript 项目中的第三方库和依赖包。

.ts 是 TypeScript 文件的扩展名。TypeScript 是 JavaScript 的超集，添加了静态类型检查等特性，帮助开发者编写更健壮的代码。

trash-cli 创建回收站

git 分支
.git/index
fork pull request pr修改逻辑
tmux（Terminal Multiplexer）是一个终端多路复用器，允许你在单个终端窗口中创建和管理多个会话、窗口和面板。它特别适合远程工作，因为会话可以持久化，即使断开连接也不会丢失。
基本使用：

启动：tmux
新建窗口：Ctrl-b c
切换窗口：Ctrl-b n（下一个）或 Ctrl-b p（上一个）
分割面板：Ctrl-b %（垂直）或 Ctrl-b "（水平）
切换面板：Ctrl-b 方向键
分离会话：Ctrl-b d（后台运行）
重新连接：tmux attach 或 tmux a
常用命令：

列出会话：tmux ls
杀死会话：tmux kill-session -t 会话名
配置：编辑 ~/.tmux.conf 添加自定义键绑定。

crontab定时任务
crontab -e  # 编辑当前用户的 crontab 文件
crontab -l  # 查看当前用户的 crontab 任务
crontab -r  # 删除当前用户的 crontab 任务
Crontab 是 Linux/Unix 系统中的定时任务调度工具，用于在指定时间自动执行命令或脚本。以下是基本用法：

基本语法
时间格式
每行任务格式：分钟 小时 日 月 周 命令

分钟：0-59
小时：0-23
日：1-31
月：1-12
周：0-7（0 和 7 都表示周日）
示例：
# 每天凌晨 1:00 备份文件
0 1 * * * /usr/bin/tar -czf /backup/backup.tar.gz /home/user/data

# 每周一早上 8:00 发送邮件
0 8 * * 1 echo "Weekly report" | mail -s "Report" user@example.com

* * * * * command：每分钟执行
0 2 * * * command：每天凌晨 2:00 执行
*/5 * * * * command：每 5 分钟执行
0 0 1 * * command：每月 1 日凌晨执行
示例任务

tar归档+压缩
归档（不压缩）：tar -cf archive.tar file1 file2 dir/
-c：创建归档
-f：指定文件名
压缩归档：
gzip：tar -czf archive.tar.gz file1 file2 dir/（常用，扩展名 .tar.gz）
bzip2：tar -cjf archive.tar.bz2 file1 file2 dir/（.tar.bz2）
xz：tar -cJf archive.tar.xz file1 file2 dir/（.tar.xz）
解压：tar -xzf archive.tar.gz（自动检测压缩类型）
-x：提取
-z：gzip，gunzip
-j：bzip2，-J：xz
v 会列出正在归档的文件名
查看内容：tar -tf archive.tar.gz
-cvzf（创建 gzip 压缩归档）
-c：create（创建归档）
-v：verbose（显示详细过程，列出文件）
-z：gzip（使用 gzip 压缩）
-f：file（指定归档文件名）
示例：tar -cvzf archive.tar.gz file1 file2 dir/

-xvzf（提取 gzip 压缩归档）
-x：extract（提取归档）
-v：verbose（显示详细过程）
-z：gzip（解压 gzip）
-f：file（指定归档文件名）