       10
      /  \
     5    15
    / \     \
   3   7     20

```
from collections import deque

class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

def build_tree_from_array(arr):
    if not arr or arr[0] is None:
        return None

    # 1. 创建根节点并放入队列
    root = TreeNode(arr[0])
    queue = deque([root])
    
    # 指针 i 用来遍历数组中的后续元素
    i = 1  
    
    # 2. 循环利用队列为每个节点装配左右子节点
    while queue and i < len(arr):
        current = queue.popleft()
        
        # 尝试构建并连接左子节点
        if i < len(arr):
            if arr[i] is not None:
                current.left = TreeNode(arr[i])
                queue.append(current.left)
            i += 1
            
        # 尝试构建并连接右子节点
        if i < len(arr):
            if arr[i] is not None:
                current.right = TreeNode(arr[i])
                queue.append(current.right)
            i += 1
            
    return root

# ---- 测试运行 ----
if __name__ == "__main__":
    data_array = [10, 5, 15, 3, 7, None, 20]
    tree_root = build_tree_from_array(data_array)
    
    # 简单验证根节点及其子节点关系
    print(f"Root: {tree_root.val}")
    print(f"Root.left: {tree_root.left.val}, Root.right: {tree_root.right.val}")
    print(f"15.right: {tree_root.right.right.val}")
    